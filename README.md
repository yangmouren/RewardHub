# RewardHub

RewardHub（中文名“带娃神器”）是一套面向家庭的本地积分管理工具。管理员可以创建管理账号和孩子账号，设置加分、扣分和兑换奖励；孩子可以自行注册账号、申请加分、兑换奖励或兑换现金，孩子提交的申请由管理员审核后生效。

系统提供家庭监控总控、账户积分表、折合金额、审核中心、积分记录和账号变更日志，并支持男孩、女孩头像和本地图标。账号、积分和操作记录保存在本地数据目录中。

当前版本：`0.13.8`

原作者： [fcyl-12/RewardHub](https://github.com/fcyl-12/RewardHub)
我根据自身需求做了些调整和修改。修改完全靠Ai。

## 本次更新（0.8.0及之后的更新都是Ai编程，基本全程workbuddy）

- 版本升级到 `0.13.6`。
- 总览横幅将「快速加积分」「快速扣积分」合并为「快速调积分」（积分页内切换加分/扣分），并新增「查看成就」入口。

- 版本升级到 `0.13.5`。
- 任务页将已完成的任务折叠归档到「已完成任务（N）」面板，支持勾选与全选批量删除。

- 版本升级到 `0.13.4`。
- 账号总览的「全部展开」按钮优化为与标题同行并靠右，窄屏不再独占一行。

- 版本升级到 `0.13.3`。
- 首页「管理账号」文字垂直居中；总览横幅动作按钮的图标与文字整体居中。

- 版本升级到 `0.13.2`。
- 首页标题栏日期与「管理账号」按钮等高对齐；发布任务表单的图标与「发布任务」按钮排在同一行。
- 任务一旦有审核通过的提交，管理端锁定编辑（重复任务除外）。
- 账号管理页改为折叠卡片，可一键全部展开/收起；商店页「当前等级优惠」「兑换现金」改为默认折叠。
- 设置页各栏目「新增」按钮回到标题右侧同一行。

- 版本升级到 `0.13.1`。
- 新增覆盖式导入家庭数据：可将此前导出的 JSON 整库恢复，导入前自动备份当前数据。
- 导入校验：仅接受已知表、只写当前版本真实存在的列、写入失败整段回滚。
- 管理工具中移除重复的「项目设置」按钮。

- 版本升级到 `0.13.0`。
- 总览移除「⋯ 更多操作」折叠，四个动作一屏平铺；「管理账号」移到页面标题右侧与日期并排。
- 项目设置改为分区 Tab（项目设置/管理工具），加/扣积分/兑换奖励三个面板改为内层 Tab 切换。

- 版本升级到 `0.12.0`。
- 大幅缩短「总览」与「项目设置」长页面：总览按 Tab 分区并折叠收纳，设置面板默认全部折叠（折叠态仍显示当前值摘要）。

- 版本升级到 `0.11.1`。
- 家长端顶部工具栏新增「设置」按钮，直达项目设置页。

- 版本升级到 `0.11.0`。
- 新增每个孩子「每日提交审核上限」，默认 10 次/天（0 表示不限制），超额当天不能再提交，次日自动恢复。

- 版本升级到 `0.10.2`。
- 优化发布悬赏面板的紫色符文位置与尺寸，避免孤立悬在说明文字旁。

- 版本升级到 `0.10.1`。
- 新增审核中心，把任务验收与积分申请汇总为统一待办列表（此前任务验收无集中入口）。
- 「记录」页改名为「审核」，并增加待办摘要。

- 版本升级到 `0.10.0`。
- 孩子端实现状态实时同步（切页/回前台/每 60 秒静默刷新），家长发布后无需刷新即可看到。
- 重复任务在生效区间内提前展示「未开始」，不再一片空白。
- 任务支持编辑与删除（此前后端接口已有，前端补全入口）。

- 版本升级到 `0.9.1`。
- 优化「兑换现金」到账预览单行显示；面板图标与卡片样式微调。

- 版本升级到 `0.9.0`。
- 新增「重复任务」：可按每天/每周/每月周期自动重复，新的一天自动出现，无需手动创建（漏做不补）。
- 发布表单提供工作日/周末/全选快捷预设，并实时生成规则文案。

- 版本升级到 `0.8.2`。
- 移除移动端底部导航「更多」浮层，商店/账号作为常规导航按钮直接并入底部导航。

- 版本升级到 `0.8.1`。
- 修复移动端底部导航「更多」浮层条目错误，管理端可见「账号」、孩子端可见「商店」。

- 版本升级到 `0.8.0`。
- 前端改为基于 URL 哈希的路由，刷新页面和分享链接可以停留在当前页面。
- 新增家庭数据导出：管理账号可将全部家庭数据导出为 JSON 备份文件。
- 新增登录失败限流，连续多次失败会临时锁定，防止暴力破解。
- 新增 PWA 支持：可安装到手机桌面，弱网或离线时仍能打开应用外壳。
- 新增真实连接状态指示，由 `/api/health` 定时轮询驱动，不再写死“数据库已连接”。
- 移动端底部导航折叠为“更多”浮层；管理表格在窄屏自动转为卡片布局。
- 新增深色模式：顶栏一键切换，偏好保存在浏览器本地。
- 新增孩子端成就解锁与升级庆祝动画。
- 安全增强：支持通过 `SECRET_KEY` 与 `COOKIE_SECURE` 环境变量配置签名密钥与 HTTPS Cookie。

- 版本升级到 `0.7.0`。
- 增加积分兑换现金，默认 `100 积分 = 1 元`，管理员可以自定义比例。
- 孩子账号可以在登录页自行注册。
- 注册账号固定为孩子账号，支持男孩、女孩头像。
- 注册后自动生成默认加分、扣分和兑换项目。
- 管理员可以继续创建、修改和删除孩子账号。
- 增加孩子自助注册日志。
- 内置生活项目图标同步到加分、扣分和兑换项目。
- 增加日常任务、史诗悬赏、任务领取和完成审核。
- 增加经验值、冒险等级、等级特权和成就勋章。
- 增加总览通知、孩子端通知弹窗和管理员公告编辑。
- 等级特权同时作用于积分赚取、奖励兑换和现金兑换。

## 孩子账号自助注册

在登录页点击“注册孩子账号”，填写登录账号、孩子名称、密码并选择头像，提交后即可进入孩子账号。孩子注册不会创建管理员账号；孩子提交的加分、奖励兑换和现金兑换申请，仍需管理员审核。

## Docker Compose 部署

准备一个目录，并在其中创建 `docker-compose.yml`：

```yaml
services:
  rewardhub:
    image: ghcr.io/yangmouren/rewardhub:latest
    container_name: rewardhub
    ports:
      - "9696:9696"
    environment:
      ADMIN_USERNAME: "${ADMIN_USERNAME:-}"
      ADMIN_PASSWORD: "${ADMIN_PASSWORD:-}"
    volumes:
      - ./points_data:/data
    restart: unless-stopped
```

启动：

```bash
docker compose pull
docker compose up -d
```

打开：`http://设备IP:9696`

更新：

```bash
docker compose pull
docker compose up -d
```

停止：

```bash
docker compose down
```

`points_data` 是数据目录。更新或重建容器时不要删除它，否则账号和积分数据会被清空。

## 首次创建管理员

首次启动且数据目录中没有管理员账号时，打开页面即可按引导创建管理员用户名和密码。

也可以在 `docker-compose.yml` 所在目录创建 `.env`，让容器首次启动时自动创建管理员：

```dotenv
ADMIN_USERNAME=your-admin
ADMIN_PASSWORD=change-this-password
```

管理员账号创建后，后续启动不会自动覆盖账号或密码。若数据目录中已经存在账号，安装向导不会重新创建管理员；需要保留数据时请使用已有账号，重新开始前请先备份数据。

## 安全与部署建议

RewardHub 默认面向家庭局域网使用，但上线到公网前请注意：

- **务必设置 `SECRET_KEY`**：会话签名密钥默认是公开占位值，公网部署前必须通过环境变量覆盖为随机长字符串，否则会话可被伪造。
- **公网请启用 HTTPS 并开启安全 Cookie**：设置 `COOKIE_SECURE=1`，让会话 Cookie 仅通过加密连接传输。
- **仅在内网或可信网络访问**：管理账号拥有全部家庭数据的读写权限，不要直接暴露在公网 9696 端口。
- **定期备份数据目录**：Docker 的 `points_data` 或二进制的 `data/` 目录包含 `points.db`，导出 JSON 也可作为额外备份。
- **修改默认管理员密码**：自动创建的管理员密码仅用于首次引导，请及时在账号管理中修改。
