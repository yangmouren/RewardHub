# Changelog

## v0.13.6

- 总览横幅：「快速加积分」「快速扣积分」合并为「快速调积分」（积分页内切换加分/扣分），新增「查看成就」入口。

## v0.13.5

**任务页：已完成任务折叠归档 + 批量删除。**

- 已完成的任务不再挤在任务看板里，统一收进底部「已完成任务（N）」折叠面板（默认收起，展开状态跨刷新保留）。
  - 管理端口径：该任务的全部提交记录都已验收通过、没有领取中/待验收/需重做的提交（日常任务做完即归档，孩子第二天还能照常领取）。
  - 孩子端口径：最近一次提交已通过。
  - 进行中的任务全部做完时，看板显示一条「进行中的任务都已完成」的提示，归档面板仍可见。
- 管理端展开归档后可勾选任务（支持「全选」）批量删除：一次确认后逐个调用删除接口，中途失败即停并提示「已删除 N / M 个」。勾选状态在后台自动刷新后不丢，已删除的任务自动清出勾选。
- 归档里的卡片淡化显示（半透明 + 图标去色），单个「删除」按钮保留。

前端有改动，`static/sw.js` 缓存名提升到 `rewardhub-v17`。

## v0.13.4

**账号总览的「全部展开」按钮与标题同行、靠右**：此前窄屏下「全部展开」掉在标题下方独占一行。根因是 ≤430px 的通用规则把 `.section-heading` 改为纵向堆叠，已针对账号总览面板覆盖为横向（`.account-overview-panel .section-heading { flex-direction: row; align-items: center; }`），按钮固定不收缩、不折行。

前端有改动，`static/sw.js` 缓存名提升到 `rewardhub-v16`。

## v0.13.3

两处居中修正：

**1. 首页「管理账号」文字垂直居中**：按钮改成 flex 布局（内容按盒子中心对齐），图标从 28px 收到 16px。此前按钮是原生 inline 布局、图标 28px 而文字 13px，两者按**基线**对齐，中文汉字面会明显低于图标中心，看上去像没居中。

**2. 总览 banner 动作按钮左右居中**：四个动作按钮（任务悬赏 / 快速加积分 / 处理审核 / 快速扣积分，孩子端同款）内容整体居中 —— 图标与文字作为一个整体居中，不再贴左边缘。实现上把「图标 + 文字 + 副标题」包成 `.button-icon` + `.button-text` 两个子元素，按钮用 `display:flex; justify-content:center; gap:8px` 排列；「处理审核」的副标题（0 条待处理）仍是块级换行，与主文字左对齐成一组。

前端有改动，`static/sw.js` 缓存名提升到 `rewardhub-v15`。

## v0.13.2

按反馈做了 6 处对齐与收纳调整：

**1. 首页标题栏对齐**：日期块与「管理账号」按钮改为等高（上下边缘齐平），此前日期块比按钮矮一截。

**2. 发布任务表单**：「任务图标」下拉不再孤零零占一格、右侧留一大块空白 —— 它与「发布任务」按钮排在同一行，图标框铺到按钮左侧，按钮贴住右边缘，顺带少一行高度。

**3. 任务验收后锁定编辑**：任务一旦有**审核通过**的提交，管理端不能再改（列表里「编辑」变成「已验收 · 不可编辑」）。已结算的奖励不会被事后改动，后端 `PUT /api/tasks/<id>` 同样返回 409。**重复任务例外**：它是长期模板，孩子每周都要重做，因此始终可编辑。

**4. 账号管理页折叠**：一个账号一张卡片，摘要只显示头像/名称/积分/待审核，点开才看登录账号、今日变化、今日提交、折合金额和编辑/删除按钮。右上角「全部展开 / 全部收起」一键切换，展开状态在后台刷新后不丢。

**5. 商店页收纳**：「当前等级优惠」「兑换现金」改为默认折叠的卡片，展开后才显示等级加成明细和兑换表单。

**6. 项目栏目标题对齐**：设置页「加积分项目 / 扣积分项目 / 兑换奖励」的「新增」按钮回到标题右侧同一行（窄屏原先会掉到第二行）。

前端有改动，`static/sw.js` 缓存名提升到 `rewardhub-v14`。

## v0.13.1

**新增：覆盖式导入家庭数据**
- 「管理工具」新增「导入家庭数据」按钮，选择之前导出的 JSON 即可整库恢复（账号、积分记录、任务、申请、项目设置、成就、公告全部覆盖）。
- **导入前自动备份**：当前整库会先另存到 `data/backups/pre-import-<时间>.json`，后悔了可以拿这份文件再导回来。
- 安全校验：只接受已知的 14 张表（未知表名直接拒绝）、只写当前版本真实存在的列（老备份缺列/多列不会整库失败）、写入失败整段回滚，数据保持原样。
- 导入的是别处的数据时，若当前登录的管理账号已不存在，会提示并用备份文件里的账号重新登录。
- 顺带修了导出的遗漏：`task_submit_log`（每日提交次数日志）此前不在导出清单里，恢复后会归零，现已补上。

**调整**：管理工具里原来的「项目设置」按钮已删除（它和顶部 Tab 重复）。

## v0.13.0

按反馈重排「总览」与「项目设置」的信息结构：去掉折叠中转，动作与配置都改成一次点击直达。

**总览 banner**
- 移除「⋯ 更多操作」折叠框，「快速扣积分」回到主动作行，四个动作（任务悬赏 / 快速加积分 / 处理审核 / 快速扣积分）一屏平铺。
- 「管理账号」移到页面标题右侧，与日期（`2026-09-12`）并排，不再藏在折叠里。
- 「管理工具」面板整体移出总览，迁到项目设置页。

**项目设置：折叠 → Tab**
- 顶部新增分区 Tab「项目设置 / 管理工具」；「管理工具」即从总览迁来的面板（账号管理 / 导出家庭数据 / 清空积分记录 / 恢复默认），内含一个「项目设置」按钮可切回。
- 「加积分项目 / 扣积分项目 / 兑换奖励」三个面板不再折叠，改为内层 Tab 切换，选中即展开整份列表，省掉「展开」这一步。
- 冒险等级、积分换钱两个表单面板保留折叠（默认收起，折叠态仍显示当前值摘要）。
- 点「新增」时会自动切到对应 Tab，保存后条目不会藏在别的分区里。

## v0.12.0

大幅缩短「总览」与「项目设置」两个长页面（实测 430px 视口下 总览 3283px → 1217px、设置 2223px → 793px）。

**总览：按 Tab 分区 + 折叠收纳**
- 顶部新增分区 Tab「概览 / 通知 / 账户」（孩子端只显示前两个），一次只看一组。
- 「通知」Tab：发布通知表单默认收起，点「＋ 发布通知」展开；进入编辑态自动展开。
- 「账户」Tab：孩子账户表的「折合金额 / 今日变化 / 状态」三列默认收起，点「展开明细」显示全部。
- banner 底部操作按钮：常显「任务悬赏 / 快速加积分 / 处理审核」，「快速扣积分 / 管理账号」收进「⋯ 更多操作」，入口一个不丢。
- RPG 冒险区的「任务完成 / 已解锁成就」两块统计收进可折叠区，默认只显示等级与 XP 进度条。
- 去重：banner 内那组「孩子账号 / 家庭积分 / 家庭折合 / 待审核」与下方「核心指标」重复，已合并——核心指标的家庭积分卡改为显示折合金额。
- 孩子端「最近申请」从概览移入「通知」Tab，概览回到 1217px。

**项目设置：五个面板默认全部折叠**
- 冒险等级、积分换钱、加积分项目、扣积分项目、兑换奖励均可折叠，**默认全部收起**。
- 折叠态仍显示当前值摘要（`自动计算：LV 3（按任务经验）`、`100 积分 = 1 元`），不展开也能读到配置。
- 折叠态点「新增」会自动展开对应面板，避免新条目藏在收起区里看不见。
- 空的项目面板自动展开，保留「暂无项目」的新增引导。

## v0.11.1

- feat(ui): 家长端顶部工具栏新增「设置」按钮（`#top-settings`，`admin-only`，孩子端不显示），点击直达「项目设置」页。此前该页面只能从首页「管理工具」进入，底部导航没有入口，路径偏深。
  - 位置放在连接状态与「修改密码」之间：孩子端该位置显示「修改密码」，家长端显示「设置」，两端视觉对称。
  - 复用全局 `data-page-target` 点击委托调 `navigate("settings")`，无需新增路由；`navigate()` 里已有 `adminPages` 守卫，即使误点也不会把孩子带进管理页。
  - 顶部「当前孩子」下拉框**保持不变**（它是管理端切换操作对象的唯一入口）。
- chore: `static/sw.js` 缓存名提升到 `rewardhub-v10`，配合 `?v=0.11.1` 刷新前端资源缓存。

## v0.11.0

- **feat: 每个孩子可配置「每日提交审核上限」，避免孩子端刷量把审核队列挤爆。** 口径是**当天提交总次数**：每点一次「提交验收」消耗 1 个名额（被退回后重新提交也算一次），用完当天不能再提交，次日自动恢复。默认 **10 次**，填 **0 表示不限制**。
  - 数据模型：`accounts` 新增 `daily_submit_limit`（老库由 `ALTER TABLE` 补列，存量孩子落到默认 10）；新增 `task_submit_log` 表逐条记录每次提交事件——**不能靠 `task_assignments.submitted_at` 计数**，因为退回后重新提交会把该字段覆盖掉，同一个 assignment 只会算一次。
  - 接口：`submit_task()` 超额时返回 **429** 与明确文案；`state_payload` 新增 `submit_quota`（`limit` / `used` / `remaining` / `unlimited`）；`account_overview_rows` 新增 `daily_submit_limit` 与 `today_submit_count`；`POST/PUT /api/accounts` 支持 `daily_submit_limit`（0–999 校验）。
  - 前端：账号弹窗新增「每日提交审核上限」输入（管理账号自动隐藏），账号管理表新增「今日提交」列（`已用 / 上限`，用满时标红）；任务页新增额度提示条（孩子看自己的剩余次数，家长看当前选中孩子的）；提交撞上限时 toast 给出原因并静默刷新额度。
  - 顺带统一：账号管理页的「待审核」列改用与总览一致的 `pendingTotal()`（积分申请 + 任务验收），此前只统计了积分申请。
- chore: `static/sw.js` 缓存名提升到 `rewardhub-v9`，配合 `?v=0.11.0` 刷新前端资源缓存。

## v0.10.2

- fix(ui): 发布悬赏面板的紫色符文 `✧` 从面板右上角移到「发布新悬赏」标题同一行（标题右侧，间距 10px），不再悬在说明文字旁边显得孤立。标题文字包进 `#quest-form-title`，因此进入编辑态时标题仍能正常切换为「编辑任务」而不会把符文一起抹掉。符文尺寸从 32px 收到 24px 以匹配 18–21px 的标题字高。
- chore: `static/sw.js` 缓存名提升到 `rewardhub-v8`，配合 `?v=0.10.2` 刷新前端资源缓存。

## v0.10.1

- **fix(重要): 孩子提交任务验收后，管理端「记录」页看不到待审核。** 根因是管理端原本没有集中的审核入口：任务提交只落在 `task_assignments.status='submitted'`，而记录页的「审核申请」板块只读 `point_requests`（赚分/兑换申请），任务验收只能在任务页每张卡片的人员小列表里逐个点。现在 `state_payload` 新增 `task_reviews` 字段（仅管理端），把待验收的任务提交汇总成一条待办列表，带「通过 / 重做」按钮，直接复用原有的任务审核接口。列表**刻意不过滤 `tasks.is_active`**：任务被撤下后，孩子已提交的那一份仍需结算，否则会永远卡在「待验收」。
- feat(ui): 「记录」页改名为「审核」并升级为「审核中心」。底部导航标签「记录 → 审核」，页面标题「积分记录 → 审核中心」，首页入口按钮「查看记录 → 查看审核」；页面顶部新增待办摘要（`待处理：任务验收 N 项 · 积分申请 M 项`），任务验收板块排在积分申请板块之前。
- fix(ui): 总览页「待审核申请」指标改为「待审核事项」，口径与审核中心一致（积分申请 + 任务验收）；账户概览表的「待审核」列改用同一口径（新增 `task_pending_count` 字段，`pending_count` 语义保持不变）。
- chore: `static/sw.js` 缓存名提升到 `rewardhub-v7`，配合 `?v=0.10.1` 刷新前端资源缓存。

## v0.10.0

- **fix(重要): 家长发布的任务，孩子端看不到。** 根因是孩子端**只在登录时拉一次状态**——`navigate()` 只重绘、不重新取数，`visibilitychange` 也只探活。所以在「家长手机发布、孩子平板查看」的典型场景里，孩子端必须整页刷新才能看到新任务。现在切换页面 / 切回前台 / 窗口重新聚焦 / 每 60 秒都会静默同步一次最新状态（8 秒节流，避免频繁请求），孩子端会自动出现新任务。
- **fix: 重复任务「今天不触发」时对孩子彻底隐形。** 之前孩子端会隐藏所有当天不触发的重复任务，于是「每周一练字」在周六打开就是一片空白，看起来像任务没发布。现在只要**计划还没结束**，就以「未开始 · 下次 09-14」的形式提前展示出来（不可领取，服务端也会拒绝提前领取）；只有生效区间已过的计划才真正隐藏。**漏做不补的规则保持不变**。
- **feat: 家长发布的任务支持编辑与删除。** 任务卡片右上角新增「编辑」「删除」按钮：编辑会把该任务回填到发布表单（标题变为「编辑任务」、按钮变为「保存修改」，另有「取消编辑」），保存走 `PUT /api/tasks/<id>`；删除走 `DELETE /api/tasks/<id>`（软删除，带确认弹窗，并提示将一并归档的领取/提交记录数量）。此前后端接口早已存在，只是前端没有入口。

## v0.9.1

- fix(ui): 「兑换现金」的到账预览不再折成两行。文案由「到账 ¥0.00 · 实际扣除 0 积分」精简为「到账 ¥0.00 · 实扣 0 积分」（优惠时附「（省 N）」），并改为 `nowrap` 单行展示；窄屏下预览独占整行，避免被挤成两行。
- style(ui): `money.svg` 从「兑换现金」面板标题右侧移到「兑换积分」输入框左侧（作为输入框内联图标），去掉重复的装饰插图，面板更紧凑。
- style(ui): 「本周积分」周一~周日卡片高度由 86px 压缩到 60px（≤430px 机型 50px），星期文字不再竖排换行，整体更紧凑。

## v0.9.0

- feat: 任务类型新增「重复任务」，可在一段时间内按周期自动重复，**新的一天自动出现，无需每天手动创建**——养成习惯不再靠手工下发。
- feat: 重复规则统一为「频率 + 星期几 + 第几个 + 生效区间」四要素，覆盖常见习惯场景：
  - `每天`：如跳绳，每天都会自动出现。
  - `每周` + 勾选星期：如练字（周一~周五）、打扫房间（周六、周日）、打篮球（周二、周四）。
  - `每月` + 第几个 + 勾选星期：如大扫除（每月第 1 个周六、周日）；「第几个」支持 1~4 与「最后一个」。
  - 生效开始/结束日期：如「21 天习惯周期」，区间外自动不再出现；留空表示长期有效。
- feat: 发布表单提供「工作日 / 周末 / 全选 / 清空」快捷预设，并实时生成规则文案（如「每月第 1 个周六、周日」）。任务卡片上也会显示该规则。
- feat: 孩子端每天打开任务页时，当天该触发的重复任务会被自动置为「已领取」，可直接点「提交验收」，仍走「提交 → 家长审核 → 发放积分」的原有流程。**漏做不补**：昨天没做的不会累积到今天，过期即消失，符合习惯养成语义。
- chore: 管理端仍可看到全部重复任务，未触发的会标注「今日不触发」并置灰，方便管理。
- fix: 修复任务被家长退回后再次点击「领取任务」会撞 `UNIQUE(task_id, account_id, claim_date)` 约束导致 500 的问题，现在会明确提示「请直接重新提交」。
- chore: `tasks` 表新增重复规则字段；老库通过 `migrate_tasks_repeat` 重建表以放开 `task_type` 的 CHECK 约束（新增 `repeat`），旧数据原样保留。service worker 缓存名提升到 `rewardhub-v4`。

## v0.8.2

- refactor: 移除移动端底部导航的“更多”浮层。此前每个角色在该浮层里其实只剩 1 个可见项（管理端=账号，孩子端=商店），入口多一次点击却毫无选择余地。现在“商店”/“账号”作为常规导航按钮直接并入底部导航，按角色自动显隐，一次点击直达。
- style: 底部导航最多 6 项（管理端），窄屏（≤560px）收紧按钮间距与图标/文字字号，保证全部项完整显示不换行。
- chore: 清理 `setupNavOverflow()` 及其相关 CSS（`.nav-more`/`.nav-overflow`/`.nav-overflow-item`/`.nav-secondary`）与 `navigate()` 中的浮层状态同步代码；service worker 缓存名提升到 `rewardhub-v3`，配合 `v0.8.2` 刷新 `?v=` 缓存键。

## v0.8.1

- fix: 修复移动端底部导航“更多”浮层条目错误——`setupNavOverflow` 之前遍历的是 `.nav-secondary` 容器而非其中的按钮，导致浮层里只生成了一个 `data-page-target="undefined"` 的“商店”条目（且不带角色类），点它会跳到一个空白页（“商店页面是空的”），同时“账号”入口完全缺失（管理端在手机上无法进入账号管理）。现改为遍历 `.nav-secondary .nav-button` 并复制 `admin-only`/`child-only` 角色类，管理端可见“账号”、孩子端可见“商店”，点击均正确跳转。
- chore: service worker 缓存名提升到 `rewardhub-v2`，配合版本号 `v0.8.1` 刷新 `app.js`/`style.css` 的 `?v=` 缓存键，避免升级后仍加载旧前端资源。

## v0.8.0

- feat: 前端改为基于 URL 哈希的路由，支持刷新/分享停留在当前页面。
- feat: 管理账号可一键导出全部家庭数据为 JSON 备份（`GET /api/export`）。
- feat: 登录增加失败限流，连续失败临时锁定，防止暴力破解。
- feat: 支持 PWA，可安装到主屏并离线打开应用外壳（manifest + service worker）。
- feat: 连接状态由 `/api/health` 真实轮询驱动，不再写死“数据库已连接”。
- feat: 移动端底部导航折叠为“更多”浮层，管理表格窄屏转为卡片布局。
- feat: 新增深色模式，顶栏切换并持久化偏好。
- feat: 孩子端成就解锁与升级增加庆祝动画。
- feat: 合并“加积分”与“扣积分”为统一的“积分”页（顶部加/扣切换，两个常用项目清单共用一页），底部导航合并为单个“积分”入口；首页“快速加/扣积分”均跳转该页。
- fix: 修复“积分”页内“扣积分”切换按钮点击无反应的问题（全局点击处理器此前未监听 `data-op-kind`，已补上切换逻辑）。
- feat: “积分”页的常用加分项目与常用扣分项目增加独立的“加分项目 / 扣分项目”分段切换，默认显示加分项目，点击即可切换查看扣分项目；两个清单容器常驻，仅切换显隐。
- feat: 合并“审核”与“记录”为统一的“记录”页：审核/我的申请区与本周积分、每日记录、月度统计同页展示；底部导航由“审核”+“记录”两个按钮合并为单个“记录”入口；旧书签 `#requests` 自动重定向到 `#records`。
- security: 支持 `SECRET_KEY` 与 `COOKIE_SECURE` 环境变量，强化会话安全。

## v0.7.0 - 2026-08-14

- fix: release without hardcoded template version (14ae04b)
- release: prepare RewardHub v0.7.0 (1abe96c)
- Merge remote-tracking branch 'origin/main' into codex/binary-deployment (9903516)
- feat: improve announcements and cash exchange preview (c40752b)

## v0.7.0 - 2026-08-14

- 增加 RPG 任务悬赏、日常任务和史诗悬赏。
- 增加经验值、冒险等级、等级特权和成就勋章。
- 增加首页通知、孩子端通知弹窗和任务完成审核。
- 增加积分兑换现金及等级折扣预览。
- 扩展生活图标库，支持分类浏览和内置搜索。

## v0.6.18 - 2026-08-09

- Merge remote-tracking branch 'origin/main' into codex/binary-deployment (6765304)
- feat: add child self-registration and cash exchange (fa6e358)

## v0.6.17 - 2026-08-07

- fix: apply install wizard admin credentials (7e9db29)

## v0.6.16 - 2026-08-07

- docs: remove binary deployment details (489b06b)

## v0.6.15 - 2026-08-07

- docs: simplify deployment README (23ce55b)

## v0.6.14 - 2026-08-07

- fix: preserve install wizard credentials (6e01c60)

## v0.6.13 - 2026-08-07

- feat: add first-run administrator setup (d134587)

## v0.6.12 - 2026-08-07

- Update README.md (97ea7b6)

## v0.6.11 - 2026-08-07

- No user-facing changes recorded.

## v0.6.10 - 2026-08-07

- fix: upload release assets with repository context (29d0763)

## v0.6.9 - 2026-08-07

- fix: preserve binary archive filenames (cd79703)

## v0.6.8 - 2026-08-07

- fix: keep release version output clean (117912b)
- feat: add binary release packaging and native confirmations (02e1a97)

## v0.6.7 - 2026-08-07

- No user-facing changes recorded.

## v0.6.6 - 2026-08-07

- Update publish-image.yml (3d6cd9d)

## v0.6.5 - 2026-08-07

- release: RewardHub v0.6.3 (9f67c9f)
- Update README.md (6b4e915)
- Update README.md (a8d2895)
- Update README.md (11d3b28)
- release: RewardHub v0.6.5 (b854457)
- release: RewardHub v0.6.5 static assets (1581b90)
- release: RewardHub v0.6.5 interface (880c080)
- test: cover RewardHub v0.6.5 (80bd914)
- assets: add RewardHub v0.6.5 family images (08be147)
- Create Makefile (45cdbb4)
- Update publish-image.yml (5e58dc0)
- Update Makefile (8954dc1)
- Create release.py (59ed613)
- Update Makefile (2ffaa08)
- Update Makefile (601f1e5)
- Update publish-image.yml (a9393c4)

